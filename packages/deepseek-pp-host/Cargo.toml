[package]
name = "deepseek-pp-host"
version = "0.1.0"
edition = "2021"
description = "Native Messaging Host for DeepSeek++ Extension"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
